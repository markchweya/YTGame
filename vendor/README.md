# Vendored libraries

- `three.min.js` — Three.js r158 UMD build (MIT, © 2010-2023 three.js authors,
  https://github.com/mrdoob/three.js). Vendored so the game runs with no build step,
  no CDN and offline. Replace with a newer build if you move to ES modules.
